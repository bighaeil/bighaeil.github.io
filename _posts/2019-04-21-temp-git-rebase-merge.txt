난 코드를 다 push 하고 개발 브런치를 rebase 했는데
push 해야 하는 것과 update를 해야하는 파일이 존재한다?
그래서 update를 하는데 충돌이 있어서 left(your code), right(branch code)를 선택해야 하는 상황이 생긴다면?

단순히 개발 브런치를 가져오면 되고
나는 해당 코드를 변경한 것도 없는데
특정 파일에서 충돌이 난다면

그건 개발 브런치를 직접 작업했기 때문.

그래서 개발 코드와 다른 브런치에서 작업한 코드가 충돌이 나는 상황

일단 나는 변경한 적이 없으면 그냥 left, right만 선택하면 되는데 무엇을 선택해야 하나

보통 merge나 rebase를 할때 충돌이 나면 right(다른 브런치)를 선택하면 되지만
이럴때는 이미 rebase를 했기 때문에 left(내 코드)가 최신 코드가 되는 것을 확인했다.

만약 이것도 귀찮다 싶으면 왼쪽이든 오른쪽이든 일단받고 "diff branch"로 비교해서 덮어 버리자.
